# Tickets — Warehouse

Local-markdown ticket store for warehouse work itself. Same conventions as the library template:

- [`templates/library/.tickets/README.md`](../templates/library/.tickets/README.md)

Currently empty. Warehouse work is tracked in [`docs/planning/future-work.md`](../docs/planning/future-work.md) until specific tickets are needed.

## Inbox

[`inbox/`](inbox/) — incoming cross-repo tickets from agents working in dependent projects (currently none expected; the warehouse has no dependents).
