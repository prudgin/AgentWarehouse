# Glossary at root, not under `docs/`

`glossary.md` lives at the repository root, alongside `CLAUDE.md` and `README.md`. The glossary is read often, edited often, and is the single most-referenced document during agent work. Top-level placement maximises discoverability and signals importance. Putting it under `docs/` would tidy the root but bury the file users need most.
