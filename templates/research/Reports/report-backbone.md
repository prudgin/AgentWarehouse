# Report backbone — <PLACEHOLDER: project name>

**The single assembly surface for the final report.** Grown incrementally from project
start (see "Report backbone" in [`../CLAUDE.md`](../CLAUDE.md)); the full scatter of
attempts — dead ends included — stays in [`../analysis/`](../analysis/analysis-landscape.md).
Only keepers are promoted here, via `/finish-analysis` ("aha → backbone").

- **Source of truth for numbers:** `pipeline/output/numbers.json` (human view: `numbers.md`).
- **Source of truth for provenance:** the per-investigation `INVESTIGATION.md` + `../analysis/analysis-landscape.md`.

How to read / maintain this file: [`README.md`](README.md).

---

## Story beats

<!-- The narrative order the report will follow. One bullet per beat; reference figures by R-id.
     This doubles as the story-spine: when a deck is built, the deck follows THIS order. -->

_None yet — fill in as the story takes shape._

## Global figure register

<!-- One row per report-candidate figure. Stable R-xx ids, never renumbered. A figure leaving
     the report is a `report: dropped` status change in the Dropped table below WITH a reason,
     never a silent deletion. -->

| R | Message | Current file | Source script | Investigation | report: |
|---|---------|--------------|---------------|---------------|---------|
| _—_ | _none yet_ | | | | |

### Dropped (recorded so the absence is intentional, not an oversight)

| R | File | Why dropped |
|---|------|-------------|
| _—_ | | |

## Headline-numbers inventory

<!-- Every number the report will state, each traced to ONE computed source
     (pipeline/output/numbers.json). No number hand-typed twice. -->

_None yet._

## Methods

<!-- Each method to be copy-pasted into pipeline/; canonical rationale stays in its ADR/domain doc. -->

_Grown as investigations surface them._

## Caveats

_Grown as investigations surface them._

## Gaps

<!-- Claims with no clean computed home yet — what the pipeline must still emit. -->

_None yet._
