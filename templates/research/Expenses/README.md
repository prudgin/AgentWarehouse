# Expenses

Finance and receipts associated with the project. Invoices, purchase orders, expense submissions.

This directory is **synced to SharePoint** at `sharepoint_planning:PROJECTS/<Project Name>/Expenses/`. If you want to keep finance off the sync entirely (sensitivity, audit), add `- /Expenses/**` to `.rclone-filter`.
